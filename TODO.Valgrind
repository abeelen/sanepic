# In order to compile sanepic for valgrind testing use

LDFLAGS="-pg" CXXFLAGS="-O0" CPPFLAGS="-pg" ./configure --disable-shared

# Then launch the standard executable normally 

valgrind --leak-check ${executable} arguments

