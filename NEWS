** SANEPIC is now on v0.2 **
- all input parameters can now be atomized by input data files (frequency sampling, high pass filter frequency, cut-off frequency for the noise-noise power spectra)
- on large datasets, one can now use FFT wisdom to optimize FFTs

