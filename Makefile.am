ACLOCAL_AMFLAGS = -I m4

if PARABOLO
 PARA = saneFrameOrder
endif

SUBDIRS = nr iniparser saneIO saneLib saneInv sanePos sanePre sanePic $(PARA) $(OPT_SUBDIRS)
DIST_SUBDIRS = nr iniparser saneIO saneLib saneInv sanePos sanePre sanePic saneFrameOrder
